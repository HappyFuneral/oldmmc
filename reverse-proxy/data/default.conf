worker_processes 1;

events {
    worker_connections 1024;
}

stream {
    server {
        listen 25565;  # ez az a port, amin kívülről jön a játékos
        proxy_pass proxy:25565
        proxy_protocol on;
    }
}
